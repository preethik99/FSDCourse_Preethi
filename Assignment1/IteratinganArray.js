//Iterating over an array means accessing each element of the array one by one based on the index of the element.
//This is commonly done using loops 
//Example
let array = [1, 2, 3, 4, 5];
for (let i = 0; i < array.length; i++) 
{
    console.log(array[i]);
}
